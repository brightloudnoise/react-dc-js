{
  "name": "react-dc-playground",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@craco/craco": "^5.9.0",
    "crossfilter2": "^1.5.2",
    "d3": "^7.6.1",
    "react": "^16.8.0",
    "react-dc-js": "file:..",
    "react-dom": "^16.13.1",
    "react-scripts": "^3.4.1"
  },
  "scripts": {
    "start": "craco --openssl-legacy-provider start",
    "build": "craco --openssl-legacy-provider build",
    "test": "craco --openssl-legacy-provider test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
